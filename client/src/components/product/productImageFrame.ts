/**
 * Shared radii for product photography — matches PDP carousel everywhere.
 * Use `full` for standalone frames; `top` where the image sits above copy in a card.
 */
export const productImageFrameFull =
  'rounded-3xl sm:rounded-[1.35rem] lg:rounded-3xl' as const

export const productImageFrameTop =
  'rounded-t-2xl sm:rounded-t-[1.35rem] lg:rounded-t-3xl' as const
